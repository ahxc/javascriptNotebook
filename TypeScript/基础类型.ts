// let s: string = "1"
// 可以通过赋值的方式省略类型标识，ts可以根据赋值获取类型。
// 养成给默认值的习惯能避免许多错误和类型体操

let s = "1"; // 字符串
let n = 0; // 数值
let t = true; // 布尔
let nu = null; // 空对象
let undef = undefined; // 未定义
let bint = 10n; // 大整型
let sym = Symbol("s"); // 符号
